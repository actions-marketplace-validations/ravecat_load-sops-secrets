name: Load SOPS secrets
description: Load secrets from a SOPS-encrypted JSON file as masked step outputs
author: Ravecat

branding:
  icon: lock
  color: blue

inputs:
  file:
    description: >-
      Absolute or workspace-relative path to a SOPS-encrypted JSON object of strings.
      SOPS is installed automatically when absent from PATH.
    required: true

  key:
    description: Age private identity text used to decrypt the file. Supports multiline identities.
    required: true

runs:
  using: node24
  main: dist/index.js
